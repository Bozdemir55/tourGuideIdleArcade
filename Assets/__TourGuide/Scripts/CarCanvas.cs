using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class CarCanvas : MonoBehaviour
{
    private GameManager _gameManager;
    private ParkPlaceInfo _parkPlaceInfo;

    public TextMeshProUGUI peopleCountText;

    void Start()
    {
//        _gameManager = FindObjectOfType<GameManager>();
//        _parkPlaceInfo = FindObjectOfType<ParkPlaceInfo>();
//        print(transform.parent.parent.gameObject.name);
//        _parkPlaceInfo.playerCollision.SetTextCanvas(transform.parent.parent.gameObject);
    }

    // Update is called once per frame
    void Update()
    {
    }
}